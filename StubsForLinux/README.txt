These Linux shared object stubs map the hardcoded library names (Windows) to the correspondent library names on Linux.
They are included in the NuGet packages and should be used automatically by .NET Core.

(Dllmap is not supported on .NET Core.)
