Only for use with mono, not for .net Core.
These config files map the hardcoded library names (windows) to the correspondent names on Linux.
Copy these files next to the dll files and mono will manage the rest.